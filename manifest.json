{
  "manifest_version": "0.4",
  "name": "neurostack",
  "display_name": "NeuroStack",
  "version": "0.13.0",
  "description": "Neuroscience-grounded knowledge management for your Markdown vault. Semantic search, knowledge graphs, AI memory, stale note detection, and 21 MCP tools.",
  "author": {
    "name": "Raphael Southall"
  },
  "icon": "icon.png",
  "server": {
    "type": "uv",
    "entry_point": "src/neurostack/server.py",
    "env": {
      "NEUROSTACK_VAULT_ROOT": "${user_config.vault_root}",
      "NEUROSTACK_API_KEY": "${user_config.api_key}",
      "NEUROSTACK_MODE": "${user_config.mode}",
      "NEUROSTACK_EMBED_URL": "${user_config.embed_url}",
      "NEUROSTACK_EMBED_MODEL": "${user_config.embed_model}",
      "NEUROSTACK_LLM_URL": "${user_config.llm_url}",
      "NEUROSTACK_LLM_MODEL": "${user_config.llm_model}"
    }
  },
  "user_config": {
    "vault_root": {
      "type": "directory",
      "title": "Vault Location",
      "description": "Path to your Markdown knowledge vault (e.g. ~/brain, ~/notes, ~/obsidian-vault)",
      "required": true,
      "default": "~/brain"
    },
    "mode": {
      "type": "string",
      "title": "Mode",
      "description": "lite = FTS5 search only (~130MB). full = adds embeddings + summaries via Ollama (~560MB). cloud = use NeuroStack Cloud for indexing.",
      "default": "lite",
      "enum": ["lite", "full", "cloud"]
    },
    "api_key": {
      "type": "string",
      "title": "Cloud API Key",
      "description": "NeuroStack Cloud API key (only needed for cloud mode). Get one at https://app.neurostack.sh",
      "required": false,
      "sensitive": true
    },
    "embed_url": {
      "type": "string",
      "title": "Embedding Server URL",
      "description": "URL of the embedding server (Ollama-compatible). Only used in full/community modes.",
      "required": false,
      "default": "http://localhost:11434"
    },
    "embed_model": {
      "type": "string",
      "title": "Embedding Model",
      "description": "Model name for generating embeddings. Must be served at embed_url.",
      "required": false,
      "default": "nomic-embed-text"
    },
    "llm_url": {
      "type": "string",
      "title": "LLM Server URL",
      "description": "URL of the LLM server for summaries, triples extraction, and RAG. Only used in full/community modes.",
      "required": false,
      "default": "http://localhost:11434"
    },
    "llm_model": {
      "type": "string",
      "title": "LLM Model",
      "description": "Model name for AI-generated summaries and RAG Q&A. Must be served at llm_url.",
      "required": false,
      "default": "phi3.5"
    }
  },
  "tools": [
    {
      "name": "vault_search",
      "description": "Hybrid FTS5 + semantic search across the vault with tiered depth control",
      "annotations": { "readOnlyHint": true, "openWorldHint": false }
    },
    {
      "name": "vault_ask",
      "description": "RAG Q&A with inline [[citations]] from vault notes",
      "annotations": { "readOnlyHint": true, "openWorldHint": false }
    },
    {
      "name": "vault_summary",
      "description": "Get pre-computed AI summary for a note",
      "annotations": { "readOnlyHint": true, "openWorldHint": false }
    },
    {
      "name": "vault_graph",
      "description": "Wiki-link neighborhood with PageRank scores",
      "annotations": { "readOnlyHint": true, "openWorldHint": false }
    },
    {
      "name": "vault_related",
      "description": "Find semantically similar notes by embedding distance",
      "annotations": { "readOnlyHint": true, "openWorldHint": false }
    },
    {
      "name": "vault_triples",
      "description": "Search knowledge graph triples (subject-predicate-object facts)",
      "annotations": { "readOnlyHint": true, "openWorldHint": false }
    },
    {
      "name": "vault_communities",
      "description": "GraphRAG global queries across topic clusters",
      "annotations": { "readOnlyHint": true, "openWorldHint": false }
    },
    {
      "name": "vault_stats",
      "description": "Index health: note count, embedding coverage, graph stats",
      "annotations": { "readOnlyHint": true, "openWorldHint": false }
    },
    {
      "name": "vault_record_usage",
      "description": "Track note hotness for excitability-weighted search",
      "annotations": { "readOnlyHint": false, "destructiveHint": false, "idempotentHint": true, "openWorldHint": false }
    },
    {
      "name": "vault_prediction_errors",
      "description": "Detect stale or poorly-indexed notes",
      "annotations": { "readOnlyHint": true, "openWorldHint": false }
    },
    {
      "name": "vault_remember",
      "description": "Save a memory with tags and entity type",
      "annotations": { "readOnlyHint": false, "destructiveHint": false, "idempotentHint": false, "openWorldHint": false }
    },
    {
      "name": "vault_forget",
      "description": "Delete a memory by ID",
      "annotations": { "readOnlyHint": false, "destructiveHint": true, "idempotentHint": true, "openWorldHint": false }
    },
    {
      "name": "vault_update_memory",
      "description": "Update an existing memory's content, tags, or type",
      "annotations": { "readOnlyHint": false, "destructiveHint": false, "idempotentHint": true, "openWorldHint": false }
    },
    {
      "name": "vault_merge",
      "description": "Merge two memories (unions tags, audit trail)",
      "annotations": { "readOnlyHint": false, "destructiveHint": true, "idempotentHint": false, "openWorldHint": false }
    },
    {
      "name": "vault_memories",
      "description": "List or search saved memories",
      "annotations": { "readOnlyHint": true, "openWorldHint": false }
    },
    {
      "name": "vault_session_start",
      "description": "Begin a memory session for grouping memories",
      "annotations": { "readOnlyHint": false, "destructiveHint": false, "idempotentHint": false, "openWorldHint": false }
    },
    {
      "name": "vault_session_end",
      "description": "End a session with optional summary and auto-harvest",
      "annotations": { "readOnlyHint": false, "destructiveHint": false, "idempotentHint": true, "openWorldHint": false }
    },
    {
      "name": "vault_harvest",
      "description": "Extract insights from recent AI coding sessions",
      "annotations": { "readOnlyHint": false, "destructiveHint": false, "idempotentHint": false, "openWorldHint": false }
    },
    {
      "name": "session_brief",
      "description": "Compact ~500 token session briefing",
      "annotations": { "readOnlyHint": true, "openWorldHint": false }
    },
    {
      "name": "vault_context",
      "description": "Assemble task-scoped context for session recovery",
      "annotations": { "readOnlyHint": true, "openWorldHint": false }
    }
  ],
  "tools_generated": false,
  "compatibility": {
    "platforms": ["darwin", "linux", "win32"],
    "runtimes": {
      "python": ">=3.11"
    }
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/raphasouthall/neurostack"
  },
  "homepage": "https://neurostack.sh",
  "documentation": "https://neurostack.sh",
  "license": "Apache-2.0",
  "keywords": [
    "knowledge-management",
    "memory",
    "semantic-search",
    "vault",
    "obsidian",
    "second-brain",
    "pkm",
    "knowledge-graph",
    "rag",
    "neuroscience"
  ],
  "privacy_policies": [
    {
      "name": "Privacy Policy",
      "url": "https://neurostack.sh/privacy"
    }
  ]
}
